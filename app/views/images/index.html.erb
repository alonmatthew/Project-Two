<%= render :partial => "partials/navbar" %>

<% if logged_in? %>
  <% @images.each do |p| %>
  <%= link_to image_path(p) do %>
  <img src="<%= p.img_url %>" class="image"></a>
  <% end %>
  <p><%= p.img_caption %></p>
  <% end %>
<% end %>
